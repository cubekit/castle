/**
 * This test checks, that the package at least compiles itself.
 */

import '../src/index'

describe('cubekit-castle', function() {
    it('must compile itself', function() {
        expect(true).to.be.ok
    })
})